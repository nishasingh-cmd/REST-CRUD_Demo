This project is a simple CRUD application built using Node.js and Express.js. 
It uses EJS templates for rendering views and demonstrates full RESTful operations:
- Create, Read, Update, Delete posts
- Dynamic routing with URL parameters
- Method override for PATCH and DELETE HTTP verbs
- Organized project structure with static assets and views
